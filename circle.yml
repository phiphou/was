machine:
   timezone: Europe/Paris
   node:
     version: 6.0.0

dependencies:
   override:
     - npm install webpack typings http-server tslint protractor -g
     - npm install

test:
   override:
     - npm run build
     - npm test
     - chmod +x ./config/test.sh
     - ./config/test.sh
